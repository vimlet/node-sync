{
  "name": "@vimlet/node-sync",
  "description": "Library that makes simple to run asynchronous functions in synchronous manner, using node-fibers.",
  "version": "1.0.0",
  "main": "lib/sync",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/vimlet/node-sync.git"
  },
  "engines": {
    "node": ">=0.5.2"
  },
  "dependencies": {
    "fibers": "^5.0.0"
  }
}
